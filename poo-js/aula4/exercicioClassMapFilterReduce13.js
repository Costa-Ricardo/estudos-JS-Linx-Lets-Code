/**
 * Use a função reduce para transformar um vetor numérico em uma string com todos os números como caractéres.
 * [1, 2, 3, 4] -> "1234"
 */

const array = [1, 2, 3, 4];

console.log(array.map(elemento => elemento.toString()));
