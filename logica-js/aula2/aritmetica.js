//Realização de calculos matematicos

/**
 * Utilize o mesmo tipo para realizar as operações 
 * padrão AMERICANO
 * regras matematicas são respeitadas / e * antes de + e - 
 */

//Operações básicas
const resultadoSoma = 10 + 10;
const resultadoSubtração = 10 - 5;
const resultadoMultiplicacao = 10 * 3;
const resultadoDivisao = 10 / 2;
const resultadoRestoDivisao = 10 % 3;

//Operações mais complexas
const valorAbsoluto = Math.abs(-10); //valor absoluto ignora as casas decimais 
const arredondaParaBaixo = Math.floor(10.5);
const arredondaParaCima = Math.ceil(10.6);

const potencia = Math.pow(2,3);
const raizQuadrada = Math.sqrt(9);

const PI = Math.PI;
const seno = Math.sin (90);
const coseno = Math.cos(90);
const tangente = Math.tan(90);

//números randomicos
const random = Math.random();

